title: ParentCaseRef
type: object
description: |-
  Ссылка на родительский случай

  🆕Добавлено в версии API 1.10
required:
  - caseNumber
properties:
  caseNumber:
    type: string
    description: Уникальный номер случая в ЛИС Лабфорс, являющийся родителем.
    pattern: ^(?!\s*$).+
    example: "5130-I11004661"
  workspaceId:
    type: number
    format: int64
    description: |-
      Идентификатор Workspace, в котором находится родительский случай.

      Необходимо заполнять только если родительский случай находится в другой лаборатории.

      Идентификаторы Workspace фиксированы для каждой лаборатории предоставляются OneCell во время настройки интеграции.
    pattern: ^(?!\s*$).+
    example: 45
